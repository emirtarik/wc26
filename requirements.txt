# Core runtime for the matchday pipeline (run_all.py):
pandas        # data layer, everywhere
numpy         # advancement / projection / odds_blend / predict_scores
pulp          # squad optimizer (optimize.py)
Unidecode     # name normalisation for player matching (match_players.py)

# Club-stats scrape (club_stats.py): runs on --refresh-club, and on the
# first run when data/processed/club_player_stats.csv is not yet cached.
soccerdata    # FBref season stats (pulls in lxml/requests as deps)
